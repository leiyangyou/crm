unless @account
  page.call "no_account_found", params[:id]
else
  page << "update_account(#{@account.to_json(
      :methods => [:last_visit_time, :active?, :transferred?, :suspended?, :expired?])})"
end