= form_for(@contract_type) do |f|
  - if @contract_type.errors.any?
    #error_explanation
      %h2
        = pluralize(@contract_type.errors.count, "error")
        prohibited this contract_type from being saved:
      %ul
        - @contract_type.errors.full_messages.each do |msg|
          %li= msg
  .field
    = f.label :name
    %br/
    = f.text_field :name
  .field
    = f.label :description
    %br/
    = f.text_area :description
  .actions
    = f.submit
