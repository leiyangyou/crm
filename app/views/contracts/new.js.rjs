page.call "crm.flip_form", :create_contract

unless params[:cancel].true?
  page[:create_contract].replace_html :partial => "form"
  page.call "crm.set_title", :create_contract
else
  page.call "crm.set_title", :create_contract
end